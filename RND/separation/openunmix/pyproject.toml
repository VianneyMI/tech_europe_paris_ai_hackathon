[project]
name = "openunmix-separation-app"
version = "0.1.0"
description = "Small CLI wrapper around Open-Unmix stem separation"
requires-python = ">=3.10"
dependencies = [
  "openunmix>=1.3.0",
]

[tool.uv]
package = false
