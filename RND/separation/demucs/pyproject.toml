[project]
name = "demucs-separation-app"
version = "0.1.0"
description = "Small CLI wrapper around Demucs 2-stem separation"
requires-python = ">=3.10"
dependencies = [
  "demucs>=4.0.1",
]

[tool.uv]
package = false
