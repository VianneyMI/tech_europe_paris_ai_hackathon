[project]
name = "spleeter-separation-app"
version = "0.1.0"
description = "Small CLI wrapper around Spleeter 2-stem separation"
requires-python = ">=3.10"
dependencies = [
  "spleeter>=2.4.0",
]

[tool.uv]
package = false
